# bookmark-grid Delta

## ADDED Requirements

### Requirement: 分组折叠记忆
用户必须能够折叠与展开任意分组（含默认分组）；折叠后该分组的书签网格隐藏、仅保留组头；折叠状态必须持久保存，刷新页面后保持。

#### Scenario: 折叠与展开
- **WHEN** 用户点击某分组的折叠按钮
- **THEN** 该分组网格立即隐藏，按钮指示切换为折叠态；再次点击恢复展开

#### Scenario: 折叠状态持久
- **WHEN** 用户折叠某分组后刷新页面
- **THEN** 该分组仍处于折叠状态
