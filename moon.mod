// mtab: MoonBit-powered browser navigation start page.

name = "2d5rrr333/mtab"

version = "0.1.0"

readme = "README.md"

repository = "https://github.com/2d5rrr333/mtab"

license = "Apache-2.0"

keywords = ["navigation", "start-page", "browser", "wasm", "lunar-calendar"]

source = "src"

preferred_target = "wasm-gc"

description = "MoonBit-powered browser start page: clock with lunar calendar, bookmark grid, multi-engine search, wallpapers — all core logic in MoonBit (wasm-gc), DOM as a thin shell."
